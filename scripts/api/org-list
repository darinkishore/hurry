#!/usr/bin/env bash
# List organizations the current user belongs to.
set -euo pipefail

source "$(dirname "$0")/_common"

check_deps
check_url

response=$(api_get "/api/v1/me/organizations")
handle_response "$response"
